{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": ". /opt/venv/bin/activate && python -m playwright install chromium && python -m playwright install-deps chromium",
    "nixpacksPlan": {
      "phases": {
        "setup": {
          "nixPkgs": ["python311", "python311Packages.pip"]
        }
      }
    }
  },
  "deploy": {
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 5
  }
}
